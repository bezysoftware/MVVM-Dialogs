<Page
    x:Class="Bezysoftware.Navigation.Dialogs.Sample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bezysoftware.Navigation.Dialogs.Sample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding Main, Source={StaticResource Locator}}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button Content="Show dialog" Command="{Binding ShowDialogCommand}" />
        <TextBlock Text="{Binding DialogResult, FallbackValue=Result}" HorizontalAlignment="Right" VerticalAlignment="Center" />
    </Grid>
</Page>
